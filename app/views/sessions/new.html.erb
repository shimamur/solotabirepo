<% provide(:title, "ログイン") %>
<div class="loginwrapper">
      <h1>ログイン</h1>
      <%= form_with(url: login_path, scope: :session, local: true) do |f| %>

        <div class="formbox">
          <%= f.label :email%><br>
          <%= f.email_field :email, class: 'form-control', value: "example@example.org" %>
        </div>

        <div class="formbox">
          <%= f.label :password%><br>
          <%= f.password_field :password, class: 'form-control', value: "foobar" %>
        </div>

        <div>
          <%= f.check_box :remember_me, class: 'form-check-input' %>
          <%= f.label :remember_me, 'ログインしたままにする', class: 'form-check-label' %>
        </div>

        <div >
          <%= f.submit "ログイン", class: "loginbtn" %>
        </div>
      <% end %>

      <p>新しくはじめる方は<%= link_to "こちら", signup_path %></p>
</div>
